import React from 'react';
import AboutScreen from '../src/about/AboutScreen';

/** /about — understand Ignova: product, sources, methodology, limits. */
export default function AboutRoute() {
  return <AboutScreen />;
}