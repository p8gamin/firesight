import React from 'react';
import AlertsScreen from '../src/alerts/AlertsScreen';

/** /alerts — FireSight's attention centre: what needs my attention right now. */
export default function AlertsRoute() {
  return <AlertsScreen />;
}