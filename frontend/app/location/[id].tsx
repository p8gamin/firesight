import React from 'react';
import LocationDetailScreen from '../../src/locations/LocationDetailScreen';

/** /location/[id] — a saved location's personalized monitoring view. */
export default function LocationDetailRoute() {
  return <LocationDetailScreen />;
}